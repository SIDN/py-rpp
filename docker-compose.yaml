services:
  rpp-app:
    container_name: py-rpp
    image: sidnlabs/py-rpp:latest
    ports:
      - "8000:8000"
    environment:
      - RPP_CONFIG_FILE=/app/config.yaml
    volumes:
      - ./config.yaml:/app/config.yaml:ro
